[project]
name = "incidents-weekly-overview"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "arrow",
    "click",
    "glom",
    "marko",
    "python-dotenv",
    "requests",
    "rich",
]

[dependency-groups]
dev = [
    "pytest",
    "responses",
    "ruff",
]
